apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mosquitto-data 
  labels:
    usage: mosquitto
  namespace: home
spec:
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-mosquitto-data
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    path: /volume1/kubernetes/mosquitto/data
    server: "${NFS_SERVER}"
